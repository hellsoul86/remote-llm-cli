# Codex App-Server Protocol Fixtures

These artifacts are pinned from the local `codex app-server` generator and committed so protocol drift is visible in code review.

Regenerate:

```bash
make protocol-schema-sync
```

Validate:

```bash
make protocol-schema-validate
```

The generator metadata is recorded in `generator.json`.
